import React from "react";
import "./playback.css";

function PlaybackPage() {
  return (
    <div id="playbackPage">
      <p>Playback page loaded</p> {/* Test content */}
      <button id="playbackBtn">Start Playback</button>
    </div>
  );
}

export default PlaybackPage;
